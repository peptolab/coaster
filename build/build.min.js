import build from './build'
import { terser } from 'rollup-plugin-terser'

export default Object.assign(build, {
  input: 'entry/entry-complete.js',
  output: Object.assign(build.output, {
    file: 'dist/coaster.min.js',
    format: 'es'
  }),
  plugins: [terser({})]
})